import { View } from 'react-native';

// Unused modal kept for expo-router's unstable_settings initialRouteName compatibility.
// Actual modals are defined in _layout.tsx Stack screens.
export default function ModalScreen() {
  return <View />;
}
